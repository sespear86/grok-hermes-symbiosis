[project]
name = "symbiosis-handoff-scaffold"
version = "0.2.1"
description = "Symbiosis handoff ops tools (scaffold + sync-report + kanban + handoff-dashboard 3694a72b)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "handoff_scaffold", "sync_report", "kanban", "handoff_dashboard"]