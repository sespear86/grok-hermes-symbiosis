[project]
name = "symbiosis-handoff-scaffold"
version = "0.3.0"
description = "Handoff scaffold (f41d2ff4) + sync report (355e3993) + handoff kanban (6239aa70) + joint projects (61cdeb81) — symbiosis scripts subtree"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "handoff_scaffold", "sync_report", "kanban", "joint_projects"]