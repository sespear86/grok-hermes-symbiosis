#!/usr/bin/env python3
"""Shim for symbiosis-mirror-audit (AUTON 9be206cf sym-build-04)."""
import sys
from pathlib import Path

root = Path(__file__).resolve().parent
sys.path.insert(0, str(root / "handoff_scaffold"))
sys.path.insert(0, str(root))

from mirror_audit.cli import main  # noqa: E402

if __name__ == "__main__":
    main()