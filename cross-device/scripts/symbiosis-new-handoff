#!/usr/bin/env python3
"""Shim for symbiosis-new-handoff (bootstrap baseline).

Delegates to handoff_scaffold.cli after full impl.
"""
import sys
from pathlib import Path

# Allow running from source tree
sys.path.insert(0, str(Path(__file__).parent / "handoff_scaffold"))

from handoff_scaffold.cli import main  # noqa: E402

if __name__ == "__main__":
    main()
