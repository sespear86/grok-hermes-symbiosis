#!/usr/bin/env python3
"""Shim for symbiosis-sync-report (AUTON 355e3993)."""
import sys
from pathlib import Path

root = Path(__file__).resolve().parent
sys.path.insert(0, str(root / "handoff_scaffold"))
sys.path.insert(0, str(root))

from sync_report.cli import main  # noqa: E402

if __name__ == "__main__":
    main()